{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "dist/app",
  "framework": "vite",
  "routes": [
    { "src": "/app/(.*)", "dest": "/dist/app/$1" },
    { "src": "/", "dest": "/index.html" }
  ]
}