{
  "version": 2,
  "builds": [
    { "src": "vite.config.js", "use": "@vercel/static-build" }
  ],
  "routes": [
    { "src": "/app/(.*)", "dest": "/dist/app/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}